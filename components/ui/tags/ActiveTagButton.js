import React from 'react';

const ActiveTagButton = () => {
    return (
        <div>
            <button className="btn btn-warning btn-xs">ACTIVE</button>
        </div>
    );
};

export default ActiveTagButton;